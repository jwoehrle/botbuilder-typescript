# Botbuilder typescript-example

An (almost) empty BotBuilder SDK (https://github.com/Microsoft/BotBuilder) example project which uses typescript.
Start the bot by invoking e.g. `yarn run start:live-debug`. 
